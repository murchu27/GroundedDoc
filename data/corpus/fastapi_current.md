# FastAPI Current Documentation

## APIRouter

`APIRouter` is the main path operation grouping utility in current FastAPI releases.
Use `include_router` to mount routers on the main application.

## Dependency Injection

Dependencies are declared with `Depends()` and are resolved per request.

## Response Model

Use `response_model` to validate and filter response payloads.
